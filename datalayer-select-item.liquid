<script>
  document.addEventListener('click', function(event) {
    var target = event.target.closest('.product-card'); // Adjust selector based on your theme
    if (target) {
      var productId = target.getAttribute('data-product-id');  // Ensure your product elements contain product data
      var productTitle = target.getAttribute('data-product-title');
      var productPrice = target.getAttribute('data-product-price');
      var productCategory = target.getAttribute('data-product-category');

      window.dataLayer = window.dataLayer || [];
      window.dataLayer.push({
        'event': 'select_item',
        'ecommerce': {
          'items': [{
            'item_id': productId,
            'item_name': productTitle,
            'price': productPrice,
            'item_category': productCategory
          }]
        }
      });
    }
  });
</script>
